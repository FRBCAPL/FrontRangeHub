# Ladder App

The Ladder app source code lives in **FrontEnd/apps/ladder/frontend/**.

This folder is kept for workspace/config purposes. When running `npm run dev` from FrontEnd, all ladder components are loaded from `FrontEnd/apps/ladder/frontend/`.
